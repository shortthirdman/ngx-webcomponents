import { AnchorLinkPipe } from './anchor-link.pipe';

describe('AnchorLinkPipe', () => {
  it('create an instance', () => {
    const pipe = new AnchorLinkPipe();
    expect(pipe).toBeTruthy();
  });
});
