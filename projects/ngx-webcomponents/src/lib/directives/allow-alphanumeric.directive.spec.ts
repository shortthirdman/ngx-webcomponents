import { AllowAlphaNumericDirective } from './allow-alphanumeric.directive';

describe('AllowAlphaNumericDirective', () => {
  it('should create an instance', () => {
    const directive = new AllowAlphaNumericDirective();
    expect(directive).toBeTruthy();
  });
});
