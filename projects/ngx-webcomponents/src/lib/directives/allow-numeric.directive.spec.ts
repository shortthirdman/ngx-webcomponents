import { AllowNumericDirective } from './allow-numeric.directive';

describe('AllowNumericDirective', () => {
  it('should create an instance', () => {
    const directive = new AllowNumericDirective();
    expect(directive).toBeTruthy();
  });
});
